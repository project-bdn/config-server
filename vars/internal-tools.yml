---
destination_folder: "{{ deployment_path }}/internal"
ansible_python_interpreter: /usr/local/bin/python3-docker # For docker swarm commands

traefik_domain: reverse-proxy.klivar.com
traefik_basic_auth_user: klivar-traefik
traefik_basic_auth_password: "{{ vault_traefik_basic_auth_password }}"

portainer_domain: docker.klivar.com

